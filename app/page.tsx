'use client';
import {useMemo,useState} from 'react'; import {Meal} from '../src/domain/types'; import {ingredients as seedIngredients,inventory as seedInventory,meals as seedMeals} from '../src/data/mockData'; import {reservedFor,availableFor,publishMeal,completeMeal} from '../src/domain/mealInventory'; import {MealBoard} from '../src/components/dashboard/MealBoard'; import {InventorySummary} from '../src/components/dashboard/InventorySummary'; import {MealForm} from '../src/components/meals/MealForm';
export default function Home(){const [meals,setMeals]=useState(seedMeals);const [inventory,setInventory]=useState(seedInventory);const [view,setView]=useState<'dashboard'|'inventory'>('dashboard');const [form,setForm]=useState(false);const [notice,setNotice]=useState('');const [filter,setFilter]=useState('全部');const save=(draft:Meal)=>{const result=publishMeal(draft,meals,inventory,seedIngredients);if(result.error){setNotice(`無法發布：${result.error}`);setMeals([...meals,draft]);}else{setMeals([...meals,result.meal]);setNotice('餐點已發布，食材已標記為保留。')}setForm(false)};const inventoryRows=useMemo(()=>seedIngredients.map(i=>({i,total:inventory.find(x=>x.ingredientId===i.id)?.quantity||0,reserved:reservedFor(meals,i.id),available:availableFor(inventory,meals,i.id)})).filter(x=>filter==='全部'||(filter==='不足'?x.available<=x.i.safetyStock:x.reserved>0)),[meals,inventory,filter]);const action=(m:Meal)=>{if(m.status==='已發布'){const done=completeMeal(m,inventory);setMeals(meals.map(x=>x.id===m.id?done.meal:x));setInventory(done.inventory);setNotice(`${m.title} 已完成，庫存已更新。`)} };return <main className="app-shell"><header className="topbar"><div className="brand"><div className="brand-mark">✦</div><div><b>MealCall</b><small>家庭餐桌</small></div></div><div className="header-right"><span className="family-label">王家冰箱</span><div className="avatar">E</div></div></header><div className="content">{view==='dashboard'?<><div className="page-heading"><div><span className="eyebrow">WEDNESDAY, JUL 29</span><h1>今天吃什麼？</h1><p>把今天的餐桌，交給家裡的每個人一起決定。</p></div><button className="primary-button" onClick={()=>setForm(true)}>＋ 安排一餐</button></div>{notice&&<div className="notice">✓ {notice}<button onClick={()=>setNotice('')}>×</button></div>}<div className="dashboard-layout"><div><div className="date-tabs"><button className="active">今天 <small>7/29</small></button><button>明天 <small>7/30</small></button><button>週五 <small>7/31</small></button><button className="calendar">▣</button></div><MealBoard meals={meals} ingredients={seedIngredients} onAction={action}/></div><InventorySummary ingredients={seedIngredients} inventory={inventory} meals={meals} onAll={()=>setView('inventory')}/></div></>:<><div className="page-heading compact"><div><button className="back-link" onClick={()=>setView('dashboard')}>← 回到看板</button><h1>全部庫存</h1><p>查看冰箱裡每一項食材的使用狀態。</p></div></div><div className="filter-tabs">{['全部','不足','已保留'].map(x=><button className={filter===x?'active':''} key={x} onClick={()=>setFilter(x)}>{x}</button>)}</div><div className="inventory-table"><div className="table-header"><span>食材</span><span>總庫存</span><span>已保留</span><span>可用量</span><span>安全庫存</span><span>狀態</span></div>{inventoryRows.map(({i,total,reserved,available})=><div className="table-row" key={i.id}><span className="food-name">{i.name}</span><span>{total} {i.unit}</span><span>{reserved} {i.unit}</span><span><b>{available}</b> {i.unit}</span><span>{i.safetyStock} {i.unit}</span><span className={available<=i.safetyStock?'warning':'good'}>{available<=i.safetyStock?'需要補貨':'庫存正常'}</span></div>)}</div></>}{form&&<MealForm ingredients={seedIngredients} onClose={()=>setForm(false)} onSave={save}/>}</div></main>}
