'use client';
import {FormEvent,useState} from 'react'; import {signIn,signUp} from '../../src/lib/supabase/auth';
export default function LoginPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [mode,setMode]=useState<'signin'|'signup'>('signin');const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMessage('');try{if(mode==='signin'){await signIn(email,password);window.location.assign('/');return}await signUp(email,password);setMessage('註冊成功，請檢查信箱完成驗證。')}catch(error){setMessage(error instanceof Error?error.message:'操作失敗')}finally{setBusy(false)}}return <main className="auth-page"><form className="auth-card" onSubmit={submit}><div className="brand-mark">✦</div><span className="eyebrow">MEALCALL FAMILY</span><h1>{mode==='signin'?'歡迎回來':'建立家庭帳號'}</h1><p>{mode==='signin'?'登入後查看今天的餐桌。':'建立帳號後加入共用冰箱。'}</p><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>密碼<input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)} placeholder="至少 6 個字元"/></label><button className="primary-button auth-submit" disabled={busy}>{busy?'處理中…':mode==='signin'?'登入':'註冊'}</button>{message&&<div className="notice">{message}</div>}<button type="button" className="text-button switch-auth" onClick={()=>setMode(mode==='signin'?'signup':'signin')}>{mode==='signin'?'還沒有帳號？建立一個':'已經有帳號？返回登入'}</button></form></main>}
