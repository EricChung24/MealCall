import {describe,expect,it} from 'vitest'; import {availableFor,publishMeal,completeMeal} from './mealInventory'; import {Meal} from './types';
const meal=(status:Meal['status']='草稿'):Meal=>({id:'x',date:'2026-07-29',slot:'晚餐',title:'測試餐',cookId:'m',status,ingredients:[{ingredientId:'egg',plannedAmount:2,reservedAmount:0,consumedAmount:0}]});
const inventory=[{ingredientId:'egg',quantity:3}];
describe('meal inventory',()=>{it('subtracts reservations from available amount',()=>expect(availableFor(inventory,[meal('已發布')], 'egg')).toBe(3));it('publishes and reserves ingredients',()=>expect(publishMeal(meal(),[],inventory,[{id:'egg',name:'蛋',unit:'顆',safetyStock:1}]).meal.ingredients[0].reservedAmount).toBe(2));it('rejects insufficient stock',()=>expect(publishMeal({...meal(),ingredients:[{ingredientId:'egg',plannedAmount:4,reservedAmount:0,consumedAmount:0}]},[],inventory,[{id:'egg',name:'蛋',unit:'顆',safetyStock:1}]).error).toBeTruthy());it('completes and consumes reserved amount',()=>expect(completeMeal({...meal('已發布'),ingredients:[{ingredientId:'egg',plannedAmount:2,reservedAmount:2,consumedAmount:0}]},inventory).inventory[0].quantity).toBe(1));});
