import {Meal,MealSlot,Ingredient} from '../../domain/types';
export function MealBoard({meals,ingredients,onAction}:{meals:Meal[];ingredients:Ingredient[];onAction:(m:Meal)=>void}){return <div className="meal-grid">{(['早餐','午餐','晚餐'] as MealSlot[]).map(slot=><section className="meal-column" key={slot}><div className="section-title"><span>{slot}</span><span className="muted">{meals.filter(m=>m.slot===slot).length} 餐</span></div>{meals.filter(m=>m.slot===slot).map(m=><article className="meal-card" key={m.id}><div className="meal-icon">{slot==='早餐'?'☀️':slot==='午餐'?'🍱':'🌙'}</div><div className="meal-content"><div className="meal-status">{m.status}</div><h3>{m.title}</h3><p className="muted">由 {m.cookId==='m2'?'媽媽':'Eric'} 發布</p><p className="ingredients">{m.ingredients.map(x=>ingredients.find(i=>i.id===x.ingredientId)?.name).join(' · ')}</p><button className="text-button" onClick={()=>onAction(m)}>查看詳情 →</button></div></article>)}{!meals.some(m=>m.slot===slot)&&<div className="empty-card">尚未安排{slot}</div>}</section>)}</div>}
